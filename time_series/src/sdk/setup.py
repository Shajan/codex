from setuptools import setup, find_packages

setup(
    name="timeseries_sdk",
    version="0.1.0",
    description="Python SDK for Time Series Prediction Platform",
    author="Your Company",
    packages=find_packages(),
    install_requires=["requests"],
    python_requires=">=3.7",
)
